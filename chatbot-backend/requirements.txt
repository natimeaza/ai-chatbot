# Core FastAPI stack
fastapi>=0.115.0          # latest stable as of early 2026
uvicorn[standard]>=0.30.0   # ASGI server (with websockets + http tools)

# For settings / .env management (very clean & recommended)
pydantic-settings>=2.5.0

# For request/response models + validation
pydantic>=2.8.0

# Optional but very useful for chatbots
python-dotenv>=1.0.0      # fallback / helper for loading .env
httpx>=0.27.0             # async HTTP client → call LLM APIs (OpenAI, Groq, etc.)

# If you later add database (SQLite for start)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0         # async SQLite driver

# Development / quality tools (strongly recommended)
ruff>=0.6.0               # linter + formatter (replaces black + flake8)
pytest>=8.3.0             # testing
pytest-asyncio>=0.21.0    # async test support